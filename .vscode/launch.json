{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug exonumist.com",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start exonumist dev server"
    }
  ]
}
